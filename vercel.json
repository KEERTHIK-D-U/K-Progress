{
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.ts"
        }
    ],
    "buildCommand": "npm run build --workspace=client",
    "outputDirectory": "dist"
}